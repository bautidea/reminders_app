// In this file im defining and exporting the common shape of a reminder
// I put this interface here because im going to use it in other parts of the
// app.
export default interface Reminder {
  id: number;
  title: string;
}
